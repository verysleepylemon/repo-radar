# Windows GNU toolchain — default toolchain is now stable-x86_64-pc-windows-gnu
# Uses WinLibs GCC for all linking (no MSVC / Visual Studio required).
# To switch back: rustup default stable-x86_64-pc-windows-msvc
